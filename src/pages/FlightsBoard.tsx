import { FC } from "react";
import TimeBoard from "../components/timeboard/TimeBoard.tsx";

const Main: FC = () => {
  return (
    <main>
      <TimeBoard />
    </main>
  );
};

export default Main;
