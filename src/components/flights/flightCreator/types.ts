export type FlightsFieldsNames = {
  flightId: string;
  airlineName: string;
  city: string;
  departureTime: string;
  checkinTime: string;
  seatCapacity: string;
  notes: string;
};

export type newFlightsFieldsNames = {
  flightId: string;
  airlineName: string;
  city: string;
  departureTime: string;
  checkinTime: string;
  seatCapacity: string;
  notes: string;
};
