export type Inputs = {
    flight_id: string;
    airline: string;
    myVal: string;
    city: string;
    flightDate: string;
    registrationDate: string;
    seatsAmount: string;
    notes: string;
};
