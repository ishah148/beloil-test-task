export type Inputs = {
    flightNo: string;
    airline: string;
    myVal: string;
    city: string;
    flightDate: string;
    registrationDate: string;
    seatsAmount: string;
    notes: string;
};
